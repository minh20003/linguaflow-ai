services:
  frontend:
    build:
      context: .
      args:
        # Public because this address is bundled into browser code at build time.
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
