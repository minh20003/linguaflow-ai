# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain
langchain>=0.3.0
langgraph>=0.2.0
# LLM providers — xem LLM_PROVIDER trong .env (langchain-openai dùng cho cả OpenAI và DeepSeek)
langchain-openai>=0.3.0
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0
# Observability cho Agent (F-03.4). API CallbackHandler khác nhau giữa các major
# version — module src/agents/observability.py viết theo v4.
langfuse>=4.0.0,<5.0.0

# Database (uncomment as needed)
# sqlalchemy>=2.0.0
# alembic>=1.14.0
# psycopg2-binary>=2.9.0

# Vector Store (uncomment as needed)
# chromadb>=0.5.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
